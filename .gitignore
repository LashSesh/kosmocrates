/target
.claude/settings.local.json
# Cargo.lock is committed: this workspace ships binaries (pse-cli,
# pse-demo, pse-traverse-cli, pse-bench-bbo, …) where reproducible
# builds are a hard requirement. Library-only forks may delete it.
*.db
*.db-journal
bench_results.json
*.swp
*.swo
*~
.DS_Store
Thumbs.db
*.zip
bench_window_ablation.json
pse-demo.json

# PSE Gemini Agent
gemini_key.txt
.env
*.key

# Python
__pycache__/
*.pyc
*.pyo
eval_bundle.json
.claude/

# nxalien-cli writes per-run output artefacts to the workspace root.
# Treat them as build output, not source. `nxalien.rules.md` is
# project rules and stays committed.
/nxalien.context.json
/nxalien.handoff.json
/nxalien.manifest.json
/nxalien.replay.json
/nxalien.signal.json

# realization benchmark runtime artifact
/bench.json
/bench*.json
