[package]
readme = "README.md"
name = "pse-traverse-cognition-cli"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "CLI for PSE-TRAVERSE-COGNITION-01 — panoptic phase cognition kernel (inspect / observe / state5 / memory-query / lattice / puzzle / panorama / calibrate / trigger / bundle / replay / verify)"

[dependencies]
pse-traverse = { path = "../../crates/pse-traverse", default-features = false, features = ["cognition", "cognition-cli"] }
serde = { workspace = true }
serde_json = { workspace = true }
serde_jcs = { workspace = true }

[[bin]]
name = "pse-traverse-cognition"
path = "src/main.rs"
